# e0001 -- first GBDT. Parent is the naive geo3 baseline (e0000), scored on the identical
# folds inside the same run so the delta is an exact paired comparison.
exp_id: e0080
parent_id: e0049
approach: gbdt_direct
change: "+funnel block: the 10 raw columns the pipeline never ingested (search/catalogue funnel, conversion ratios)"
tier: confirm
seed: 0
es_gap_days: 30
fixed_rounds: 178

feature_blocks: [base, counts, rank, visit, channel, dispersion, sbcnomoment, funnel]
max_train_anchors: null

num_boost_round: 3000
early_stopping_rounds: 100
lgb_params:
  objective: regression
  metric: rmse
  learning_rate: 0.05
  num_leaves: 63
  min_data_in_leaf: 200
  feature_fraction: 0.8
  bagging_fraction: 0.8
  bagging_freq: 1
  lambda_l2: 1.0
  max_bin: 255
  num_threads: 16
  verbosity: -1

notes: "search_to_ord Spearman 0.5668 vs to_ord 0.5731 at anchor 2025-10-16; 10 unused cols add +0.00489 incremental R^2 over the 4 used ones (linear proxy). gmv_search/gmv_cat gave GMV by channel; these give ORDER COUNTS by channel + conversion rates."
