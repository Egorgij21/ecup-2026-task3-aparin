# e0187 -- frozen-fold arch search, fold-4 rank 8 of 31 (fold4 1.73329).
#
# STAGE 2 OF A TWO-TIER DESIGN (CLAUDE.md 4.2), added after the first attempt selected on ONE
# fold and did not generalise: e0180/81/82 all beat e0101 on fold 4 and all three were worse on
# folds 0-2, because 31 trials against a single fold had room to fit that fold's noise.
# The screen (fold 4) proposes; the 5-fold confirm disposes. Selection happens on the CONFIRM.
exp_id: e0187
parent_id: e0101
approach: nn_seq
change: "arch-search rank 8: gru d64 L4 dropout 0.39 lr 3.97e-03 wd 3.94e-04 batch 256 8 epochs"
tier: confirm
seed: 0
arch: gru
d_model: 64
n_blocks: 4
dropout: 0.3929
dow: false
epochs: 8
batch_users: 256
lr: 3.973238e-03
weight_decay: 3.939703e-04

notes: "stage-2 confirm; screened on fold 4 only, selection deferred to the 5-fold mean"
