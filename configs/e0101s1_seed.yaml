# e0101s1 -- e0101 replicate, seed 1. Noise-floor measurement for the seq approach.
# README.md requires sigma_noise per model family, and the GBDT's 0.00009 says nothing
# about a network: SGD order, dropout masks and init all move here and none of them move a
# boosted tree. These runs double as members of a multi-seed average.
exp_id: e0101s1
parent_id: e0101
approach: nn_seq
change: "e0101 replicate, seed 1 -- seq noise-floor measurement"
tier: confirm
seed: 1
arch: gru
d_model: 128
n_blocks: 2
dropout: 0.1
dow: false
epochs: 12
batch_users: 512
lr: 0.003
weight_decay: 0.0001
notes: "sigma_noise for nn_seq; also a multi-seed ensemble member"
