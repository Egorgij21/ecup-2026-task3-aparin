# e0106 -- parent e0101, ONE change: epochs 12 -> 30.
exp_id: e0106
parent_id: e0101
approach: nn_seq
change: "GRU epochs 12 -> 30 (cosine schedule rescaled), everything else identical to e0101"
tier: confirm
seed: 0
arch: gru
d_model: 128
n_blocks: 2
dropout: 0.1
dow: false
epochs: 30
batch_users: 512
lr: 0.003
weight_decay: 0.0001
notes: "training-length lever on the winning backbone; e0101 runs in 3.7 min so this is nearly free"
