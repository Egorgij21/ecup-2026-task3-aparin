# Путь A — сборка и проверка сабмита из предсказаний компонентов.
# Ничего тяжёлого: ни torch, ни lightgbm здесь не нужны.
# Версии — те, на которых путь A проверялся (Python 3.9.6).
numpy>=1.24,<2.0
polars>=1.0
pandas>=2.0
pyarrow>=14.0
