# Путь B — обучение всех 13 моделей с нуля.
#
# На кластере это были ТРИ отдельных окружения, чтобы зависимости не конфликтовали:
#   envs/ecup — LightGBM + Optuna, CPU-партиция
#   envs/seq  — torch + CUDA, GPU-партиция
#   envs/ag   — AutoGluon (тянет свой набор пинов, конфликтующий с остальными)
# Здесь они сведены в один файл. Если pip ругается на конфликт — разводите по окружениям,
# слоты обучаются независимо друг от друга и обмениваются только csv/parquet файлами.

-r requirements.txt

pyyaml>=6.0
scipy>=1.10

# слот GBDT
lightgbm>=4.0
optuna>=3.4                 # только для повторения поиска гиперпараметров e0093;
                            # сами параметры зафиксированы в configs/e0093_lgb3_params.json
autogluon.tabular>=1.0      # член e0064

# слоты SEQ и USERCV
torch>=2.1                  # проверялось на 2.5.0 + CUDA 12
